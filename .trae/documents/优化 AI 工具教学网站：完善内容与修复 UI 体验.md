## 1. 内容调研与数据补充
- **调研详情**：深入补充 OpenCode CLI、LLM 家族（MiniMax, Grok, Kimi 等）以及 IDE 特定模型（Composer-1, SWE-1.5）的技术细节。
- **更新 tools.json**：
  - **新增工具**：添加 OpenCode CLI。
  - **完善模型家族**：补充 GPT, Gemini, MiniMax, Kimi, Grok 家族及其官网。
  - **添加 IDE 模型**：在对应 IDE 下补充 Composer-1 和 SWE-1.5 的描述。
  - **调整评分**：将 Codebuddy 设为 4 星，Kiro 设为 3 星。
  - **趣味标签**：为每个工具新增 `fun_ranking` 字段（展示“从夯到拉”排名）。

## 2. 网站 UI 优化与 Bug 修复
- **修复内容遮挡**：
  - 调整工具卡片的布局，增加 `hover` 态的展示逻辑，确保长文本内容（Insights/Pros/Cons）在鼠标移入时能完整显示。
  - 修复 `line-clamp` 导致的文本截断问题，采用 `overflow-y-auto` 或动态展开方案。
- **展示劣势内容**：在工具卡片中新增“劣势 (Cons)”展示区块，与“优势 (Pros)”并列。
- **修复星级显示**：
  - 优化星级渲染组件，支持半星展示或更清晰的视觉反馈。
  - 修复可能存在的星级容器宽度不足导致的显示不全问题。
- **添加趣味排名标签**：在卡片显著位置（如标题旁）展示“从夯到拉”的趣味评价标签，增强互动性。

## 3. 验证与交付
- **预览验证**：运行 `npm run dev`，在预览界面检查：
  - 各分类下的新工具是否展示正确。
  - 鼠标悬停时内容是否不再遮挡且能看全。
  - 优劣势对比是否清晰。
  - 趣味标签和修正后的星级是否符合预期。
- **文档更新**：同步更新离线 Markdown 文档，确保内容一致性。