---
title: 2026-02-02 - 算法题目精解：字符串处理与限流算法
cover: https://images.unsplash.com/photo-1555066931-4365d14bab8c?w=800&h=400&fit=crop&crop=center
theme: lapis
---

# 2026-02-02 - 算法题目精解：字符串处理与限流算法

## 📄 内容概要

本文档整理了两道经典算法题目：字符串数值提取和QPS滑动窗口限速器。第一题考察字符串处理和边界条件判断，第二题考察滑动窗口算法在实际限流场景中的应用。两道题目都提供了完整的Java实现和复杂度分析，适合面试准备和算法学习。

![算法编程](https://images.unsplash.com/photo-1555066931-4365d14bab8c?w=800&h=400&fit=crop&crop=center)

---

## 题目1：字符串数值提取

### 问题描述

从字符串中提取所有整数（含负数），返回最大值。

**示例**：`'afeag-123fae+023faeb+*098'` → 最大值：98

### 解题思路

使用双指针遍历字符串，识别数字和负号，解析整数并更新最大值。需要注意以下关键点：

1. **符号识别**：负号后面必须紧跟数字才视为负数
2. **数字解析**：逐位累加构建完整数值
3. **边界处理**：处理字符串末尾和连续非数字字符

### 核心代码实现

```java
public static int findMaxNumber(String str) {
    int max = Integer.MIN_VALUE, i = 0, n = str.length();
    while (i < n) {
        if (Character.isDigit(str.charAt(i)) || 
            (str.charAt(i) == '-' && i + 1 < n && Character.isDigit(str.charAt(i + 1)))) {
            int sign = str.charAt(i) == '-' ? -1 : 1;
            if (sign == -1) i++;
            int num = 0;
            while (i < n && Character.isDigit(str.charAt(i))) {
                num = num * 10 + (str.charAt(i++) - '0');
            }
            max = Math.max(max, sign * num);
        } else {
            i++;
        }
    }
    return max;
}
```

### 复杂度分析

- **时间复杂度**：O(n)，n为字符串长度，每个字符最多访问一次
- **空间复杂度**：O(1)，只使用常数级别的额外空间

---

## 题目2：QPS滑动窗口限速器

### 问题描述

给定滑动窗口大小 n（秒）、QPS限速值 m、请求时间戳列表，计算被限速的请求数量。

### 解题思路

使用队列维护窗口内的请求时间戳，遍历时移除超出窗口的旧请求，判断当前窗口内请求数是否超限。

**核心思想**：
1. **窗口维护**：队列存储窗口内所有请求的时间戳
2. **过期清理**：每次处理新请求前，移除超出窗口的旧请求
3. **限流判断**：窗口内请求数达到限流值时，新请求被限速

### 核心代码实现

```java
public static int getQueuedRequestCount(int windowSize, int qpsLimit, List<Long> requests) {
    if (requests == null || requests.isEmpty()) return 0;
    Collections.sort(requests);
    Queue<Long> window = new LinkedList<>();
    int queued = 0;
    long windowMs = windowSize * 1000L;
    for (long requestTime : requests) {
        while (!window.isEmpty() && requestTime - window.peek() > windowMs) {
            window.poll();
        }
        if (window.size() < qpsLimit) {
            window.offer(requestTime);
        } else {
            queued++;
        }
    }
    return queued;
}
```

### 复杂度分析

- **时间复杂度**：O(n log n)，n为请求数量，排序占主导
- **空间复杂度**：O(n)，队列最多存储n个请求

---

## 💭 总结与思考

| 题目 | 核心技巧 | 考察点 |
|------|----------|--------|
| 字符串数值提取 | 双指针遍历、符号识别 | 字符串处理、边界条件 |
| QPS限速器 | 滑动窗口、队列应用 | 限流算法、数据结构 |

这两道题目分别考察了**字符串处理**和**滑动窗口算法**两个重要的算法技巧。字符串数值提取题重点在于边界条件的处理，特别是负号的识别；QPS限速器题则展示了滑动窗口在实际工程中的应用，是分布式系统中常见的限流策略。

在面试中，除了提供正确的代码实现，还需要清晰地说明解题思路和复杂度分析，展示对算法本质的理解。

![数据结构与算法](https://images.unsplash.com/photo-1509228468518-180dd4864904?w=800&h=400&fit=crop&crop=center)
